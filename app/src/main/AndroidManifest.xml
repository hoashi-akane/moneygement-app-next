<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moneygement">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".controller.DispSavingsHistoryActivity">
        <activity android:name=".NewMemberConfirmActivity"></activity>
        <activity android:name=".NewMemberActivity">

        <activity android:name=".DispSavingshistoryActivity" />
        <activity android:name=".ThirdActivity"></activity>
        <activity android:name=".SecondActivity" />
          
        <activity android:name=".controller.InputSpendingExpenseAmountActivity" />
        <activity android:name=".controller.DispExpensesCalendarActivity" />
        <activity android:name=".controller.InputSavingAmountActivity" />
        <activity android:name=".controller.DispCalendarActivity"></activity>
        <activity android:name=".controller.MainActivity">
          <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
          </intent-filter>
        </activity>
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>
</manifest>
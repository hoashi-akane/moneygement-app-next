query shareLedgers(
    $userId: Int!
){
    ledger{
        shareLedgers(userId: $userId){
            id
            adviserId
            groupId
            name
            created_at
        }
    }
}